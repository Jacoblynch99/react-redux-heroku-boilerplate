import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
    avatarIcon: {
        margin: 10,
        flexGrow: 1,
    },
    navTitle: {
        flexGrow: 1,
    },
    menuIconStyle: {
        margin: 10,
    },
})

export default useStyles
